# Power BI Desktop
*.pbix
*.pbip.tmp
**/.pbi/localSettings.json
**/.pbi/cache.abf

# Data — never commit a real export
Data/
*Identified*.zip
ConsumptionDashboard-*.zip
PersonServiceCreditsMetrics.csv
PeopleMetaData.csv
PersonPolicyMap.csv
SpendingPolicyMetadata.csv
StudioTenantDaily.csv
StudioPerAgent.csv
StudioPerUser.csv
GitHubAiUsage.csv
GitHubUserMap.csv
entra_org.csv

# ...but the synthetic sample set is meant to be here
!1. Local CSV/sample-data/*.csv

# OS / editor
Thumbs.db
.DS_Store
desktop.ini
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Fabric notebook checkpoints
.ipynb_checkpoints/

# PBIP working projects. The deliverables are the .pbit files, not the projects.
# A project keeps parameter VALUES on disk, so committing one publishes whatever
# tenant identifiers or local paths were last used. A .pbit does not - template
# export strips every parameter value. Two projects reached a commit this way.
*.pbip
*.Report/
*.SemanticModel/
